apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: lamp
spec:
  type: LoadBalancer   # o NodePort, según tu uso
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: web
